// import { Balance } from "@/app/lib/object-definitions";
//
// export const calculateTotalBalance(balances: Balance[]) => {
//     return balances.reduce((total, balance) => {
//         const balanceValue = parseFloat(balance.amount_value); // Convert string to number
//         return total + balanceValue;
//     }, 0);
// }